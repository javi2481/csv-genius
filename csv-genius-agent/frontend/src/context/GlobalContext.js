// GlobalContext.js
// Contexto global para manejar estados compartidos en la app (opcional, a completar si se necesita).

import { createContext } from 'react';

const GlobalContext = createContext();

export default GlobalContext; 